const { default: Component } = require("../../../../core/Component");
import './settings.css'

class Settings extends Component {
    constructor(id) {
        super(id);
        this.insertHTML(html);
    }
}

const html = '<div class="settings" id="settings"></div>'
    

export default Settings